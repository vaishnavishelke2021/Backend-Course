<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Chat</title>
    <link
      href="https://fonts.googleapis.com/css?family=Lato:100,100italic,300,300italic,regular,italic,700,700italic,900,900italic"
      rel="stylesheet"
    />
    <style>
      ::-webkit-scrollbar {
        width: 5px;
      }
      ::-webkit-scrollbar-thumb {
        background-color: rgb(30, 91, 111);
        border-radius: 10px;
      }
      body {
        background-color: rgb(142, 170, 180);
        color: #161616;
        font-family: "Lato";
      }

      #wrapper {
        width: 90%;
        height: 95vh;
        margin: auto;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
      }

      #wrapper div {
        color: rgb(12, 62, 79);
        width: 32%;
      }

      #wrapper div h3 span{
        color: #161616;
        font-size: 14px;
        font-style: italic;
      }

      #wrapper div p{
        color: #161616;
        margin-top: -5px;
        font-weight: 400;
        font-size: 17px;
      }


      form {
        width: 35%;
        padding: 1rem;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
      }

      input,
      textarea {
        width: 90%;
        padding: 10px;
        border: none;
        outline: none;
        border-radius: 5px;
      }

      button {
        margin-top: 2rem;
        width: 95%;
        background-color: rgb(30, 91, 111);
        padding: 8px 0px;
        border-radius: 5px;
        color: #f6f6f6;
        border: none;
        outline: none;
        font-size: 1rem;
      }

      button:hover {
        background-color: rgb(12, 62, 79);
        color: #ffffff;
        transition: all 0.3s;
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <div id="wrapper">
      <div>
        <h3>Edit your chat: <span>Chat ID: <%= updatedChat._id %></span></h3>
        <p>
          This chat was sent from <b><%= updatedChat.from %></b> to <b><%= updatedChat.to
          %></b>
        </p>
      </div>
      <form action="/api/v1/chats/<%= updatedChat._id %>?_method=PUT" method="POST">
        <textarea rows=14" cols="10" name="message">
<%= updatedChat.message %></textarea
        >
        <button>Save changes</button>
      </form>
    </div>
  </body>
</html>
